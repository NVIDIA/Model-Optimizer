# SPEED-bench MTP speculative-decoding run for Qwen3.5-4B via vLLM.
#
# Companion to specdec_bench.yaml (the NONE/autoregressive baseline). This
# variant exercises Qwen3.5-4B's built-in MTP head (text_config.mtp_num_hidden_layers=1)
# with draft_length=3 to produce real acceptance-rate numbers instead of the
# trivial AR=1 that NONE yields.
#
# Slurm run on cw_dfw:
#   uv run slurm.py --yaml modules/Model-Optimizer/tools/launcher/examples/Qwen/Qwen3.5-4B/specdec_bench_mtp.yaml --yes

job_name: Qwen3.5-4B_specdec_bench_mtp

pipeline:
  global_vars:
    hf_model: /hf-local/Qwen/Qwen3.5-4B

  task_0:
    script: common/specdec_bench/run.sh
    args:
      - --dataset speed
      - --dataset_path /hf-local/nvidia/SPEED-Bench-Internal/qualitative
      - --engine VLLM
      - --speculative_algorithm MTP
      - --draft_length 3
      - --tp_size 1
      - --ep_size 1
      - --concurrency 1
      - --num_requests 80
      - --output_length 4096
      - --aa_timing
      - --show_progress
      - --save_dir /scratchspace/specdec_bench_mtp
      - --trust_remote_code
    environment:
      - HF_MODEL_CKPT: <<global_vars.hf_model>>
      - HF_LOCAL: /hf-local
      # Provenance env vars consumed by dump_env() in examples/specdec_bench/run.py.
      # These are static here for demonstration; a launcher auto-injection
      # (planned for Phase-2 in OMNIML-4788) will compute them at launch time
      # so the YAML stays SHA-free.
      - SPECDEC_BENCH_SHA: "62d1686570448e8c4c057592f45c23a5965ec206"
      - MODELOPT_SHA: "58633f0a6021a0ca9f250ca53e2090d2ebfea182"
      - MODELOPT_VERSION: "0.45.0dev-108-g58633f0a60"
      - NMM_SANDBOX_SHA: "079b41847544cf142e25820197294eea94ad216e"
      - CONTAINER_IMAGE: "vllm/vllm-openai:qwen3_5-cu130"
    slurm_config:
      _factory_: "slurm_factory"
      nodes: 1
      ntasks_per_node: 1
      gpus_per_node: 1
      container: vllm/vllm-openai:qwen3_5-cu130
