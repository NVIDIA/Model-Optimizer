# Schema validation error: sparsity = 0.0 out of range
_test:
  description: "Schema error: sparsity 0.0 rejected (must be > 0)"

  validate:
    expect_error: "Sparsity must be in"

version: "1.0"
sparsity:
  method: sparse_gpt
  sparsity: 0.0
