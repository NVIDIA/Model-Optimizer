# @package _global_

defaults:
  - full_vlm_smoke_extended
  - /families/qwen3_5/qwen3p5_0p8b/vlm_quality_evaluation@_global_
  - _self_

# Deterministic bounded regression for the expanded multi-axis smoke candidate.
puzzle_dir: ${oc.env:PUZZLETRON_RUN_ROOT,puzzle_runs/qwen3p5_0p8b_e2e_vlm_quality_comparison_extended}
display_name: Qwen3p5 0.8B expanded multi-axis VLM quality comparison

post_mip:
  flows:
    params-90:
      nodes:
        quality_benchmarks:
          type: downstream_evaluation
          input: short_vlm_kd
          failure_policy: strict
          config: ${vlm_quality_evaluation}
