# Copy this file to my_paths.yaml and edit only the values in this section.
defaults:
  - /families/qwen3_5/qwen3_5_9b/full_pipeline@_global_
  - _self_

paths:
  modelopt_root: /path/to/engineering/modelopt_qwen
  automodel_root: /path/to/engineering/Automodel
  vllm_root: /path/to/engineering/vllm
  aiperf_root: /path/to/engineering/aiperf
  dataset_path: /path/to/engineering/Puzzle-KD-Nemotron-Post-Training-Dataset-v2
  puzzle_root: /path/to/persistent/puzzle_runs

clean_config_root: ${paths.modelopt_root}/examples/puzzletron/configs/clean
puzzle_root: ${paths.puzzle_root}
dataset_path: ${paths.dataset_path}

aiperf:
  executable: ${paths.aiperf_root}/.venv/bin/aiperf
