# @package _global_

defaults:
  - /families/qwen3_5/qwen3_5_9b/sanity_check@_global_
  - _self_

# This campaign intentionally excludes embedding-width pruning from MIP.
embedding_pruning:
  enabled: false
  widths: [4096]

mip:
  canonical_solutions_path: ${to_path:${replacement_scoring.subblock_solutions_path}}
